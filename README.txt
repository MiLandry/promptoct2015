Directions:

  unzip to filesystem
  run maven command "mvn install" in root of workspace to compile executable jar, which will be place in the target directory
  run the jar file:
  	java -jar promptoct2015-0.0.1-SNAPSHOT.jar